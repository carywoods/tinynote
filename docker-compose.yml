services:
  tinynotes:
    image: php:8.3-apache
    restart: unless-stopped
    environment:
      APP_PASSWORD: "tammy2026"
    volumes:
      - ./index.php:/var/www/html/index.php:ro
      - ./data:/var/www/html/data
